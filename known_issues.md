# سجل المشاكل المعروفة في Trade Lak Bot

## المشاكل التي ظهرت وتم حلها

### 1. حد الصفقات المفتوحة يمنع الفرص الجديدة
- **المشكلة:** MAX_SPOT_TRADES=3 ثم =5 كان يمنع البوت من فتح صفقات رغم وجود رصيد حر وفرص جيدة
- **السبب:** البوت يعد الصفقات المفتوحة ويرفض الدخول إذا وصل الحد
- **الحل:** رفع MAX_SPOT_TRADES إلى 15
- **الفحص عند التشغيل:** التحقق أن MAX_SPOT_TRADES >= عدد العملات في المحفظة

### 2. المزامنة تُحذف عند إعادة التشغيل
- **المشكلة:** المزامنة كانت تعتمد على ملف portfolio_sync.pkl مؤقت يُحذف بعد التحميل الأول
- **السبب:** `os.rename(sync_file, sync_file + '.loaded')` يجعل الملف غير متاح في إعادة التشغيل التالية
- **الحل:** استبدال الملف المؤقت بدالة `_auto_sync_portfolio()` تجلب من OKX مباشرة عند كل تشغيل
- **الفحص عند التشغيل:** التحقق أن عدد الصفقات في open_spot_trades == عدد العملات في OKX

### 3. رصيد منخفض وهمي يوقف البوت
- **المشكلة:** البوت يرى الرصيد الحر فقط ($1.37) ويتوقف، بينما الأموال موزعة في عملات
- **السبب:** get_balance() يُعيد usdt_free فقط دون احتساب قيمة العملات المحتفظ بها
- **الحل:** إضافة فحص `usable < 5` يتجاهل العملات المفتوحة عند حساب الرصيد المتاح
- **الفحص عند التشغيل:** التحقق أن الرصيد المُعاد > 0 أو أن هناك صفقات مفتوحة بالفعل

### 4. direction خاطئ في trade_record المزامَن
- **المشكلة:** المزامنة كانت تضع direction='BUY' بدلاً من 'SPOT_BUY'
- **السبب:** خطأ في كتابة القيمة في _auto_sync_portfolio
- **الحل:** تصحيح إلى direction='SPOT_BUY' ليتوافق مع check_exit_conditions
- **الفحص عند التشغيل:** التحقق أن كل trade في open_spot_trades له direction في ('SPOT_BUY','LONG','SHORT')

### 5. amount_coin مفقود في trade_record المزامَن
- **المشكلة:** _execute_exit تحاول `trade['amount_coin']` لكن المزامنة لم تضعه
- **السبب:** نسيان إضافة amount_coin في بناء trade_record
- **الحل:** إضافة `'amount_coin': qty` في trade_record
- **الفحص عند التشغيل:** التحقق أن كل trade له amount_coin > 0

### 6. open_time مفقود في trade_record المزامَن
- **المشكلة:** _execute_exit تحسب `duration_min` من open_time لكنه غير موجود
- **السبب:** نسيان إضافة open_time في بناء trade_record
- **الحل:** إضافة `'open_time': datetime.now()` في trade_record
- **الفحص عند التشغيل:** التحقق أن كل trade له open_time من نوع datetime

### 7. Telegram HTML parsing errors
- **المشكلة:** رسائل Telegram تفشل بسبب أحرف HTML غير مُعالَجة في أسماء العملات أو الأسباب
- **السبب:** بعض النصوص تحتوي على < > & بدون html.escape
- **الحل:** تطبيق html.escape على كل النصوص الديناميكية في رسائل HTML
- **الفحص عند التشغيل:** إرسال رسالة اختبار HTML عند البدء والتحقق من نجاحها

### 8. FakeBreakDetector يكتشف عند المقاومة لا الدعم
- **المشكلة:** البوت يكتشف Liquidity Grab عند المقاومة (إشارة بيع) لكن نادراً عند الدعم (فرص شراء)
- **السبب:** السوق في وضع عرضي والأسعار بعيدة عن مناطق الدعم القوية
- **الحل:** هذا سلوك صحيح — الانتظار حتى يصل السعر لمنطقة دعم حقيقية
- **الفحص عند التشغيل:** التحقق أن FakeBreakDetector مُهيَّأ ويستجيب للبيانات

### 9. العملات الصغيرة القيمة (< $5) تُتجاهل
- **المشكلة:** ADA وSUI لم تُحمَّل في المزامنة لأن قيمتهما أقل من $5
- **السبب:** حد التجاهل كان $5
- **الحل:** تخفيض الحد إلى $1 لمراقبة جميع العملات
- **الفحص عند التشغيل:** التحقق أن عدد العملات المزامَنة == عدد العملات في OKX بقيمة > $1

### 10. الحد الأدنى للصفقة على OKX
- **المشكلة:** البوت يحاول فتح صفقة بأقل من الحد الأدنى ($10) فيرفضها OKX
- **السبب:** حساب حجم الصفقة يعطي مبلغاً صغيراً جداً عند رأس مال منخفض
- **الحل:** إضافة فحص `if amount_usdt < 10: skip`
- **الفحص عند التشغيل:** التحقق أن MIN_ORDER_USDT = 10 في config

### 11. البوت يتوقف عند 3 صفقات (قديم)
- **المشكلة:** MAX_SPOT_TRADES=3 كان الإعداد الافتراضي
- **السبب:** إعداد محافظ جداً
- **الحل:** رفع إلى 15
- **الفحص عند التشغيل:** قراءة MAX_SPOT_TRADES من config والتحقق أنه >= 10
